#directory "_build/lib"

open JavaScript

let parse path =
    (try 
       parse_javascript_from_channel (open_in path) path
     with exn ->
      (Format.printf "Exception parsing %s (%s)\n%!" path (Printexc.to_string exn);
       exit 2))

let rslt = parse "tests/parse/add.js";;
